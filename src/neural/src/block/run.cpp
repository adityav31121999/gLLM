
#include "include/block.hpp"

void block::runBlock(std::vector<std::vector<float>>& tokenEmbed, int& tokenCount, int& blockCount) 
{
    // using blockCount and head indexes, extract caches
    // then layer-wise perform forward propagation
    // calculate token embedding
}
