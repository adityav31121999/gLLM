
#ifdef USE_OPENCL

#include "include/block.hpp"

void block::clInferParallel(const mat& tokens, int& in, int& tokenCount, int& layers, int& parallelNumber) 
{
    //
}


void block:: clInferParallel(std::vector<mat>& expectedV, const mat& tokForBlock, int& in, int& tokenCount, 
    int& blockCount, int& layers, int& n, int& parallelNumber) 
{
    //
}

#endif
